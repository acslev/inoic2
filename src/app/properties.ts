import {OpaqueToken} from '@angular/core';

export const propertiesToken = new OpaqueToken('propertiesToken');

export const properties = {
  API_URL: "http://localhost:8080/",
  PROP_1: "prop_1"

};
